[project]
name = "netbridge-pentest"
version = "1.0.0"
description = "Penetration testing suite for NetBridge"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.3",
]

[project.scripts]
netbridge-pentest = "pentest_suite:main"
